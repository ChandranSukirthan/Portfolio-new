import * as lucide from "lucide-react";
console.log("Keys containing face:", Object.keys(lucide).filter(k => k.toLowerCase().includes("face")));
console.log("Keys containing linked:", Object.keys(lucide).filter(k => k.toLowerCase().includes("linked")));
console.log("Keys containing hub:", Object.keys(lucide).filter(k => k.toLowerCase().includes("hub")));
